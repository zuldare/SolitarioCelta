<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Material.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:actionBarStyle">@style/SCeltaThemeActionBar</item>
    </style>

    <style name="SCeltaRadioButton" >
        <item name="android:layout_width">@dimen/button_side</item>
        <item name="android:layout_height">@dimen/button_side</item>
        <item name="android:onClick">fichaPulsada</item>
    </style>

    <style name="SCeltaThemeActionBar" parent="@android:style/Widget.Holo.Light.ActionBar" >
        <item name="android:navigationBarColor">@android:color/holo_blue_dark</item>
        <item name="android:actionMenuTextColor">@android:color/black</item>
    </style>

</resources>
